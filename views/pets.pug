//- ** This is a sample of a pug template and how it uses the data passed to it from index.js.

doctype html
html 
    head 
        title= `${title}`
        meta(name="viewport" content="width=device-width, initial-scale=1")
        link(rel="stylesheet", href="/css/style.css")
    body    
        h1 Pets
            p 
                a(href='/update-pets') Add new Pet
        .cards 
            each pet in data
                #{pet.properties.name}
                .card 
                    h2.card__name #{pet.properties.name} - #{pet.properties.age}
                    p Type: #{pet.properties.type}